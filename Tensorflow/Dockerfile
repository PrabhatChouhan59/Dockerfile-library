# Use the official TensorFlow image with Python and dependencies
FROM tensorflow/tensorflow:latest

# Set working directory
WORKDIR /app

# Copy all project files
COPY . .

# Run the model script
CMD ["python", "model.py"]
